Description
PrivilegeAuditor is a tool that scans the system for SUID files and other potential security vulnerabilities to analyze privilege escalation risks.

Features
Detects files with SUID set.
Compares with the GTFOBins database.

Future features to be added:
Detection of additional privilege escalation vectors.

Installation
To run the project, follow these steps:

Clone the repository:

git clone https://github.com/your_username/PrivilegeAuditor.git
cd PrivilegeAuditor

Install the required Python libraries:
pip install -r requirements.txt

Usage
Start PrivilegeAuditor:
python privilege_auditor.py

Outputs
After the scan is complete, the files with potential privilege escalation risks will be listed on the screen.

